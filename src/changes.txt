###############################################################################
# 6/9/14
###############################################################################
- Refactored Google API authentication code in gapi.py in order to separate
  authentication for the native CLI app from authentication for the webapp.
    - Client secrets JSON for native application now at
      src/data/native_client_secrets.json.  Webapp client secrets now at
      src/data/webapp_client_secrets.json.

- Installed:
    - Flask-GoogleLogin

- TODO
    - Handle non-existent users.  At present, non-existent users are treated as
      being free for the entire interval of interest.

- Notes
    - For documentation: OAuth2WebServerFlow should take 'hd':
      'oregonstate.edu' argument to restrict logins to Oregon State email
      addresses.  However, doing this causes an HTML 400 error.
