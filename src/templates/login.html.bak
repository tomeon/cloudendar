{% extends "base.html" %}
{% import "bootstrap/wtf.html" as wtf %}

{% block page_content %}
{{ macros.page_header("Login") }}

<div class="row">
    <div class="col-md-12">
        {{ wtf.quick_form(form, form_type="horizontal", horizontal_columns=('md', 2, 10), button_map={'submit': "primary"}) }}
    </div>
</div>

{% endblock %}
